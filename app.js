let mode='top';const items=[];function render(){const q=document.getElementById('search').value.toLowerCase();const feed=document.getElementById('feed');const data=items.filter(x=>(mode==='all'||x.top)&&x.title.toLowerCase().includes(q));feed.innerHTML=data.length?data.map(x=>`<article class="news"><small>${x.source} • ${x.published}</small><h3>${x.title}</h3><a href="${x.url}" target="_blank" rel="noopener">OPEN ORIGINAL SOURCE ↗</a></article>`).join(''):'<div class="card">Waiting for live original-news source connection.</div>';}render();